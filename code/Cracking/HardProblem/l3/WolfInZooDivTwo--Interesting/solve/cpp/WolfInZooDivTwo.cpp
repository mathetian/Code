#include <vector>
#include <string>
using namespace std;

#define FF(i,n) for(i=0;i<n;i++)

/*int count(int N, vector<string> const &L, vector<string> const &R) 
{
	string strl, str2;int i,j;vector<int> l1,r1;
	FF(i,L.size()){ str1+=L.at(i); str2+=R.at(i); }
	l1=str1.split();r1=str2.split();
	vector<int>l2,r2;

    return 0;
}
*/
int count(int N, vector<string> const &L, vector<string> const &R) 
{
	return 0;
}